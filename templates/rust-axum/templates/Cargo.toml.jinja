[package]
name = "{{ package_name }}"
version = "{{ version }}"
edition = "2024"
description = "Generated Axum API"

[dependencies]
axum = "0.8.7"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
default = []
{%- for tag in tags %}
{{ tag }} = []
{%- endfor %}
